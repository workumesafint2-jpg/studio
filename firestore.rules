rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    /**
     * ITB Pro: Institutional Security Rules v35.0
     * Fully permissive for institutional testing and data recovery.
     */
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}