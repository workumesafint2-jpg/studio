rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    /**
     * Institutional Security Rules v18.0
     * Optimized for zero-cost deployment and maximum resilience.
     */
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}