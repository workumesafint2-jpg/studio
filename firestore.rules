
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    /**
     * Institutional Security Rules v9.0
     * Wide Open for Production Resilience as per user request.
     */
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
